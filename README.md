# 🍭 Parte Teórica – Introducción a las Aplicaciones Web

## 1. ¿Qué es HTML y cuál es su función en la web?
HTML es un lenguaje que se usa para crear la estructura de las páginas web. Su función es organizar el contenido como títulos, párrafos, imágenes, enlaces y botones para que el navegador los pueda mostrar correctamente.

## 2. ¿Qué es una etiqueta HTML y menciona las etiquetas más comunes?
Una etiqueta HTML es una instrucción que le dice al navegador cómo mostrar el contenido. Las más comunes son:

- `<html>`: indica el inicio del documento.  
- `<head>`: contiene información del documento.  
- `<body>`: contiene el contenido visible.  
- `<h1>`: se usa para títulos.  
- `<p>`: se usa para párrafos.  
- `<a>`: se usa para enlaces.  
- `<img>`: se usa para imágenes.  

## 3. ¿Qué es un atributo de una etiqueta HTML y menciona los más comunes?
Un atributo es una característica que se le agrega a una etiqueta para darle más información. Los más comunes son:

- `href` en los enlaces.  
- `src` en las imágenes.  
- `alt` para describir imágenes.  
- `class` y `id` para estilos y scripts.  

## 4. ¿Qué es CSS y cómo se utiliza para el diseño web?
CSS es un lenguaje que se usa para darle diseño a las páginas web. Sirve para cambiar colores, tamaños, fuentes y organizar los elementos en la página.

## 5. ¿Qué es una propiedad en CSS y menciona las propiedades más comunes?
Una propiedad en CSS es lo que se quiere cambiar de un elemento. Las más comunes son:

- `color`: cambia el color del texto.  
- `background-color`: cambia el color del fondo.  
- `font-size`: cambia el tamaño del texto.  
- `text-align`: alinea el texto.  
- `margin`: controla el espacio externo de un elemento.  
- `padding`: controla el espacio interno del elemento.  
